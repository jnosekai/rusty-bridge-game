# GREEN BOAT ゲーム開発 共通ルール

このリポジトリで機能追加・修正を行うすべての作業では、以下を必ず守る。

## 実装方針

- 既存の正常動作している機能を壊さない。
- ゲームバランスの細かな数値を独断で作り込まない。
- 後から調整する数値は、変更しやすい設定値として処理本体から分離する。
- 新しい機能は、将来の追加・差し替え・拡張がしやすい構造を優先する。
- 依頼範囲外の機能を不用意に追加・変更しない。

## 実装後の確認

- 実装後は、実際のブラウザで操作して動作を確認する。
- 変更した機能だけでなく、船移動、速度、燃料、ゲーム内時間、HIT、タイミングゲージ、ファイト、釣果、帰着、GAME OVERなど、影響を受ける主要機能を回帰テストする。
- 問題を発見した場合は自分で修正し、修正後に同じ操作を再テストする。
- 正常動作を確認するまでGitHubへ保存しない。
- コード以外の文書だけを変更した場合は、ブラウザでのゲーム操作確認は不要。ただし、差分と保存内容を確認してからGitHubへ保存する。

## GitHubへの保存

- 検証用に一時変更した確率・時間・魚サイズなどは、必ず通常設定へ戻す。
- 意図したファイルだけが変更されていることを確認する。
- 検証完了後にコミットし、`main` ブランチをGitHubへ反映する。

## 完了報告

最後の報告は、次の3項目だけを簡潔に記載する。

- 変更内容
- 確認した動作
- 残っている問題
